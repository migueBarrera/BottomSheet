<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SampleBottomSheet"
             mc:Ignorable="d"
             x:Class="SampleBottomSheet.MainPage">

    <Grid>
        <Grid.GestureRecognizers>
            <SwipeGestureRecognizer Swiped="SwipeGestureRecognizer_Swiped" Direction="Down"/>
        </Grid.GestureRecognizers>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Button Text="Click Me!" Command="{Binding ClickCommand}"/>
        </StackLayout>

        <local:SimpleBottomSheet x:Name="SimpleBottomSheet" IsOpen="{Binding IsOpen}"/>
    </Grid>

</ContentPage>
